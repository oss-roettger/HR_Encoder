This folder contains the calculated StyleGAN2 latent codes w (encoded images).
Latent codes w are vectors with a dimension 18x512 (ffhq model) down to 14x512 (cat model).
Use the StyleGAN2 generator to display as an image.
